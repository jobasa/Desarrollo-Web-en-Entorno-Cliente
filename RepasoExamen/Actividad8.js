/*Crea una función llamada multipleFactorial que tome como parámetro un array de número llamado dataArray,
y devuelva un núevo array que sea el resultado de calcular el factorial para cada uno de los elementos en el array.
Emplea funciones de orden superior (e.g., map, filter, reduce, etc.)*/

dataArray = [1, 2, 3, 4, 5];

function multipleFactorial(dataArray) {
    for (let numeros of dataArray) {
        numeros = dataArray*=i;
        return numeros
    }
}

console.log(multipleFactorial(dataArray));
